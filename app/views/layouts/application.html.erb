<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Social Movies" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Social Movies" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'layouts/navigation' %>
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
        </div><!--/row-->

        <footer>
            <p class="text-center">&copy; Web Engineering Mini-project - 2015</p>
        </footer>
    </div> <!-- /container -->
  </body>
</html>
