<h1>Search#movie</h1>
<p>Find me in app/views/search/movie.html.erb</p>

<h2>Search results</h2>


<% if !@movies.nil? %>
  <table>
  <% @movies.each do |movie| %>
    <tr>
      <td><%= movie['title']['value'] %></td>
      <td><%=
      if movie.has_key? 'releaseDate'
        movie['releaseDate']['value']
      end %></td>
      <td><%= link_to 'Show', movie_path(movie['id']['value']) %></td>
    </tr>
  <% end %>
  </table>
<% else %>
  <p>No results</p>
<% end %>
